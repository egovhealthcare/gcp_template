apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dcm4chee.fullname" . }}-ohif-config
  labels:
    {{- include "dcm4chee.labels" . | nindent 4 }}
data:
  app-config.js: |
    {{- .Files.Get "files/app-config.js" | replace "DICOM_BASE_URL_PLACEHOLDER" .Values.dicomBaseUrl | nindent 4 }}
