{{- if and .Values.configMap.enabled (ne (len .Values.configMap.data) 0) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "metabase.fullname" . }}-config
  labels:
    {{- include "metabase.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}

